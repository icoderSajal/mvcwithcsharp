@model inventorySystem.Models.tbl_purchase

@{
    ViewBag.Title = "DeletePurchase";
}

<h2>DeletePurchase</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>tbl_purchase</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayName("PO Name")
        </dt>

        <dd>
            @Html.DisplayFor(model => model.poName)
        </dd>

        <dt>
            @Html.DisplayName("PO Date")
        </dt>

        <dd>
            @Html.DisplayFor(model => model.poDate)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.proQty)
        </dt>

        <dd>
            @Html.DisplayName("PO Qty")
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

    <div class="form-actions no-color">
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        @Html.ActionLink("Cancel", "DisplayPurchase", "Purchase", new { @class = "btn btn-primary" })
    </div>
    }
</div>
